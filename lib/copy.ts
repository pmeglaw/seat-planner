// Shared user-facing copy constants (2026-07-16 detail critique, action 10).
// The publish-impact reassurance appeared in ten phrasings across dialogs;
// the pure-generic sites share this one sentence. Dialogs whose warning
// carries action-specific content (CSV import, deactivation, restore — all
// pinned by the destructive-action safety tests) keep their own wording.
export const PUBLISH_IMPACT_NOTE = "Viewers keep seeing the published map until you publish this draft.";

# CLAUDE.md

Read `AGENTS.md` at the repository root before working on Seat Planner. It is the shared source of project rules, architecture, design requirements, safety boundaries, and verification guidance for Claude and Codex.

Use the task-specific guides referenced there instead of duplicating their instructions here. When working under `app/concepts/`, also read `app/concepts/CLAUDE.md` for the prototype-specific restrictions.

Maintain shared guidance in `AGENTS.md`; keep this file as the entry point for Claude. Task completion does not authorize committing, pushing, deploying, or cleaning unrelated work.

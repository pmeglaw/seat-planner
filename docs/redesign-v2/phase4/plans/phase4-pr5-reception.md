# Phase 4 · PR 5 — Reception + route surfaces — BUILD HAND-OFF (fresh session)

You are Claude Code building Phase 4 PR 5 of the Seat Planner IBM/Carbon redesign. The plan below was written by a previous session, reviewed by the design-governance reviewer against the record, and **approved by the owner with seven rulings** (the "Rulings" section). This is the "go". Do not re-plan; build it as written plus the rulings.

## 0. Set up before touching anything

1. `git checkout main && git pull` — expect `4a1b861` ("PR 4 merged (v1.75.0)") on top of `18f855d`; tag `v1.75.0` on `18f855d`; tree clean; no feature branches.
2. Verify the `ibm-design-language` skill fingerprint with the PHASE3DS §0 recipe — expect **`f997ee525800e755`**. A different value stops the session; report it.
3. Read, in order: `CLAUDE.md` (Design system + Brand System LOCKED); `docs/redesign-v2/phase4/PHASE4BUILD.md` §1 (esp. §1.5, §1.19 O5, §1.22, §1.28, §1.35–§1.39), §2, slice log; `phase4/TEST-TRIAGE.md` (PR 5 rows); `DECISIONS.md` D3 + D3′ + D3-a…e, D4, deviation 12, §6 nos. 16–17; `PHASE2UX.md` §1R, §2 Reception rows, §3, §5 item 5; `phase3/PHASE3DS.md` §1.22, §1.29, §2 Reception/route rows, §5 (items 4, 5, 18; PR 4/5 landing files; retired names), §7; specimen `phase3/specimens/04-forms-and-tables.html` (#reception, #route, #narrow); wireframe `docs/redesign-v2/wireframes/reception.html`. Off-limits inputs: `docs/design-system/AUDIT*`, `PLAN.md`, `shell-reference.html`, the `docs/redesign` branch, `app/concepts/`.
4. **Task 0:** create the branch `feat/phase4-reception` and save this whole document as the plan of record at `docs/redesign-v2/phase4/plans/phase4-pr5-reception.md` (first commit: `docs(redesign-v2): phase 4 — PR 5 plan of record`). PHASE4BUILD §1's PR 5 stub points at it.

## 1. Standing constraints (every task)

- Never edit `carbon-tokens.css` / `carbon-components.css`. `sp-components.css` / `sp-tokens.css` change **only in lockstep** with `docs/redesign-v2/phase3/{components,tokens}/` (byte-identical test); each change is a dated "Phase 4 PR 5 amendment" paragraph in PHASE3DS §1. **Expected in PR 5: one sheet change (amendment E), zero token changes.**
- Brand colour only from `app/styles/brand/megeredchian-law-tokens.css`; no new hex; `HEX_LEDGER` ends with **two** reasoned permanent rows (`app/layout.tsx`, `components/seat-map/SeatSheet.tsx` — ruling Q-3); no `--cds-*` outside sp-tokens / assets / brand / the font bridge; retired names stay retired; the token test only gains assertions (the `shadow-sp` ban), never loses one.
- Guardrail tests never loosened — re-point / rewrite only per TEST-TRIAGE; `reception-source`'s published-layer-only pins and the D3′ string pins stay verbatim.
- Local dev writes to production: every capture, seed, role, spec run and smoke on the **local Docker stack** (`.env.local` re-pointed for the run, restored after). Never Publish / Discard / Restore / Import / Delete on the Vercel preview. The smoke hand-off comes from the reviewer after Task 11; the owner (or the reviewer's read-only rig) walks the preview; "merge" comes from the owner.
- Not in PR 5 (parked, untouched): the narrow tearsheet's content-height question; a distinct SQLSTATE for `deactivate_employee`; the Help-panel opener for the Ask Planner popover; the 900px `panel:` breakpoint and `SEAT_CENTER_PANEL_BREAKPOINT_PX`; `.sp-callout`; the three stale remote branches; the CLAUDE.md "Design system" rewrite; concepts marked superseded; the seven map confirm dialogs (→ PR 5b, ruling Q-5); v2.0.0.
- Merge = **v1.76.0** (annotated; `package.json` stays `0.1.0`). Branch `feat/phase4-reception` · PR title `feat(redesign-v2): phase 4 — PR 5 Reception + route surfaces`.

## 2. Rulings (owner, 2026-09-06 — fold each into the plan where named)

1. **Q-1 Esc — two rungs.** A typed query: Esc clears the query; the lock and the readout stay (PHASE2UX §1R.6: the readout keeps the last locked person — the call may still be live). An empty field with a lock: Esc unlocks and removes `?q=`. Amend P3-18's wording in PHASE3DS §5 item 18 and PHASE4BUILD §2 to "Esc clears a typed query; on an empty field it unlocks", dated, citing §1R.6 and the specimen's two hints.
2. **Q-2 — yes.** `app/error.tsx` and `app/not-found.tsx` join the `.sp-route-card` pass in Task 6: same card, same layer-02 rule, the same `chunk-recovery` anchors for the boundary; copy stays as shipped (voice per surface). Add both to the file map and to the `screenshots/pr5/` capture list (the 404 is drivable — any unknown URL; the root boundary is not — ct only, say so in the README).
3. **Q-3 — option (3).** `/my-seat` has no Phase 2 wireframe and no Phase 3 specimen, so a re-skin would be a Phase 4 design decision (PHASE4BUILD: "Phase 4 makes no design decisions"); the slice log already says "confirmed unchanged". `HEX_LEDGER` ends with two reasoned permanent rows — `app/layout.tsx` (themeColor) and `components/seat-map/SeatSheet.tsx` (deviation 12: the share card is the one surface off the token system, byte-identical by capture). Re-word the SeatSheet row's comment from "PR 5" to the permanent reason; a PHASE4BUILD §1 entry names it. No token change; the contrast line reads "no token change".
4. **Q-4 — no status mark on Reception rows.** PHASE3DS §1.29 governs; correct §5 item 5's consumer list ("Reception rows" struck, dated) and close P3-5's Reception half as "no mark drawn, §1.29".
5. **Q-5 — PR 5b.** The seven map confirm dialogs move to the asset modal on the PR 4 `CarbonModal` host in their own slice after PR 5 and before PR 6 (`feat/phase4-map-dialogs`, v1.77.0; PR 6 becomes v2.0.0 after it). In PR 5 touch them only for `shadow-sp` and the × glyph. Record O-8 in PHASE4BUILD §1 as a PR 3 obligation found open, assigned to PR 5b; add slice-log row 5b "not started".
6. **Q-6 — accept.** The 32px `.sp-page` padding sits inside the 1584 live area (PHASE3DS §1.22, shipped for Management and Settings in v1.75.0). The page-frames spec asserts the readout's 480 and the 32 gap; PHASE2UX §1R.2 gains a dated amendment line ("list 1008 at 1920 — the live area's 32 padding is inside the 1584").
7. **Q-7 — copy accepted with one edit.** Title "Seat locations didn't load"; body "Extensions are up to date. Seat and floor details will show after a reload." Warning kind, `role="status"`.

Stop at the end of Task 11 with the gate output and the preview link; do not open the PR until the reviewer's smoke has run and passed.

---

# The approved plan (v1, 2026-09-05) — build this

## State (verified 2026-09-05)
- `main` = `4a1b861` "docs(redesign-v2): phase 4 — PR 4 merged (v1.75.0)" on top of `18f855d` (#519, squash). Tag **v1.75.0** points at `18f855d`. Tree clean; no feature branches; no worktrees.
- PHASE4BUILD slice log: rows 0–4 merged; row 5 "not started"; §2 open items = **P3-18**, **P2-5**, the Reception halves of **P3-4**, **P3-5**, **P2-4**.
- `HEX_LEDGER` = 3 rows (`app/global-error.tsx` 10, `app/layout.tsx` 1, `components/seat-map/SeatSheet.tsx` 12). `SWEPT` = {1,2,3,4}; bridge §2 empty.
- `shadow-sp`: **4 sites** left of PR 1's 40 — `app/(shell)/admin/error.tsx:49`, `app/(shell)/admin/page.tsx:18` (the shipped `/admin` 403 card — never rebuilt: PHASE3DS §2 row 55 assigned it to PR 3), `components/seat-map/SeatMapDialogs.tsx:79` (Vacate) and `:221` (Discard draft). Definition in `tailwind.config.ts` `boxShadow.sp`.
- `components/ui/CloseIcon.tsx`: **7 consumer sites in 5 files** — `EmployeesTable.tsx:210` (toolbar clear ×), `AskPlannerDrawer.tsx:285` (Close Ask Planner), `SeatInspector.tsx:971` (Close inspector), `SeatInspector.tsx:1448` (the move-conflict dialog's ×), `SeatMapDialogs.tsx:160` (Delete seat ×), `:349` (Swap ×); plus the retiring `tests/close-icon-source.test.mjs`. `components/seat-map/mapIcons.tsx` already exports a 16-grid `CloseIcon` (the specimen's `#i-close`) consumed by `MapSearch`, `MapControlRow`, and — cross-area — `EmployeesTable`, `OptionList`, `DepartmentCombobox`.
- Reception today: `components/reception/ReceptionScreen.tsx` (333 lines, Tailwind, 1060 frame, avatars via `buildInitials`, `aria-selected` used for the HIGHLIGHT, no `?q=`, no clear ×, no Ctrl K); `app/(shell)/reception/page.tsx` (`Promise.all` → any query failure throws to `app/error.tsx` "The seat map could not load" — the wrong voice, D3-e); `loading.tsx` 720-wide grey bars. `/reception` has no `error.tsx`. The sheet already ships every `.sp-recep*` / `.sp-readout*` / `.sp-row-button*` / `.sp-route-card` / `.sp-digest` rule (blocks 27–28) and the Reception tokens (`sp-tokens.css` 457–532) — **but no narrow-fold rule for `.sp-recep`** (the specimen's one-column frame is page-local CSS).
- `tests/e2e/viewport-matrix.spec.ts` already walks `/reception` (signed-out redirect at four widths) — no change needed there; the signed-in frames go to e2e-auth.
- The seed (`supabase/seed.sql`) gives the four seated people extensions **201–204**; the other eight have none — enough for the no-extension state, the unique-match landing (`?q=201`) and the fallback rows.

## File map
| Area | Create | Modify | Delete |
|---|---|---|---|
| Reception | `components/reception/ReceptionFrame.tsx` (server: `.sp-page` + `.cds-page-header`, shared by page / loading), `app/(shell)/reception/error.tsx` | `components/reception/ReceptionScreen.tsx` (rebuilt on `.sp-recep`), `app/(shell)/reception/page.tsx` (`?q=`, `Promise.allSettled` partial, `.sp-page` region), `app/(shell)/reception/loading.tsx` (skeleton on the real layout) | — |
| Route cards | `components/ui/ErrorGlyph.tsx` (the specimen's `#i-error20`, inlined — PHASE3DS §7 item 6), `app/fonts/plex.ts` (the two `localFont` calls, moved so `global-error` can share them) | `app/(shell)/admin/error.tsx`, `app/(shell)/admin/page.tsx` (403 branch only), `app/global-error.tsx`, **`app/error.tsx`, `app/not-found.tsx` (ruling Q-2)**, `app/layout.tsx` (imports the fonts from `app/fonts/plex.ts`; nothing else) | — |
| Carry-ins | — | `components/seat-map/SeatMapDialogs.tsx` (2 × `shadow-sp` removed; 2 × glyph import), `components/seat-map/SeatInspector.tsx` (2 × glyph import), `components/seat-map/AskPlannerDrawer.tsx` (glyph import), `components/admin-management/EmployeesTable.tsx` (glyph import), `tailwind.config.ts` (`boxShadow.sp` + comment) | `components/ui/CloseIcon.tsx`, `tests/close-icon-source.test.mjs` |
| CSS (lockstep) | — | `app/styles/sp-components.css` block 27 **amendment E** + `docs/redesign-v2/phase3/components/sp-components.css` identical. **No `sp-tokens.css` change.** | — |
| `/my-seat` | — | `components/seat-map/SeatSheet.tsx` — **no change** (ruling Q-3); only the ledger comment is re-worded | — |
| Tests | `tests/e2e-auth/reception-keyboard.spec.ts` | `tests/reception-screen.test.mjs` (rewrite), `tests/reception-source.test.mjs` (re-point), `tests/chunk-recovery-boundary-source.test.mjs` (+ reception boundary), `tests/phase4-token-layer-source.test.mjs` (ledger; `shadow-sp` ban; font-pin re-point), `tests/touch-target-source.test.mjs` (Reception comment block), `tests/e2e-auth/page-frames.spec.ts` (Reception block), `tests/e2e-auth/header-geometry.spec.ts` (`/reception`, viewer), `tests/e2e-auth/accessibility.spec.ts` (`/reception` rest + locked; `/admin` 403 as viewer) | `tests/close-icon-source.test.mjs` |
| Rigs | `docs/redesign-v2/phase4/screenshots/pr5/README.md` | `audit/runtime-audit.mjs` (`PAGE_ROUTES` += `/reception`; `/my-seat` + `/login` before/after captures), `audit/page-states.mjs` (Reception section, viewer session; the 404) | — |
| Docs | `docs/redesign-v2/phase4/plans/phase4-pr5-reception.md` (this file) | PHASE4BUILD §1 (entries below), §2 (P3-4, P3-5, P3-18, P2-4, P2-5), §3 PR 5 row, §4 line, slice log (+ row 5b); PHASE3DS §1.29 amendment E paragraph, §5 item 5 + item 18 corrections, §5 landing note; PHASE2UX §1R.2 amendment line (Q-6), §1R.4 item 1 amended (no avatar); DECISIONS D3-e "built" line, D4 + deviation 12 "confirmed unchanged (PR 5)" lines; TEST-TRIAGE "PR 5 outcomes" | — |

## Record items → design, files, test tier
Tiers: **unit** `npm test` · **source** `tests/*-source.test.mjs` · **ct** `npm run test:ct` (jsdom) · **e2e** backend-free Playwright · **e2e-auth** Docker stack · **rig** `phase4/audit/*.mjs` on the Docker stack.

1. **Frame** (PHASE2UX §1R.2; D3-a; PHASE3DS §1.22). `ReceptionFrame.tsx` = `div.sp-page.mx-auto.w-full` > `div.cds-page-header` > `h1.cds-page-title` "Reception" + `p.cds-page-subtitle` "Front-desk directory — type what the caller gives you, read the extension, transfer." — **no `.cds-btn` in the header**. `page.tsx`: `main.flex.min-h-0.flex-1.flex-col…lg:overflow-hidden` > the focusable scroll region (`role="region" aria-label="Reception directory" tabIndex=0 lg:overflow-y-auto`, the PR 4 page pattern) > `ReceptionFrame` > `ReceptionScreen`. The sheet's `.sp-recep` grid gives list `minmax(0, 1072)` · readout 480 · gap 32 and the readout's sticky `top: calc(--sp-shell-header-h + 16)`; in the shell the **pane** scrolls at `lg`, so the readout sets `lg:[--sp-shell-header-h:0px]` on itself (PR 4 §1.37's tab-strip trick; no sheet change). **O-1 (ruling Q-6, accepted):** the content box is 1520 and the list measures 1008 at 1920, the readout 480. *Tests:* e2e-auth `page-frames` Reception block at 1920 / 1280 / 1024 (no sideways scroll; zero `.cds-btn` in the header; readout width 480 and gap 32 at the two wide frames; one column at 1024; skip link lands on the field); rig runtime audit (0 undefined `var()`).
2. **Search** (PHASE3DS §1.29, §5 item 4; D3-b; P3-4's Reception half). `div[role="search"]` > `.sp-search-lg` > `SearchIcon` (`mapIcons`) + `input.cds-text-input` `type="search" role="combobox" aria-expanded aria-controls="reception-results" aria-activedescendant aria-label="Search the directory"` (visually unlabelled — the placeholder "Name, department, seat, or extension…" is the label) `autoFocus autoComplete="off" spellCheck={false}` **`id="reception-main"`** (the skip link lands on the field, §1R.7 — `shellNavConfig`'s href and the `nav-shell` probe unchanged, the marker div goes) + `.sp-search-trailing` > `span.sp-kbd[aria-hidden]` {hint} + (query ? `button.cds-btn.cds-btn--icon.sp-search-clear` `aria-label="Clear search"` `onMouseDown={preventDefault}` → clears the query only : null). Hint: `useState("Ctrl K")` → effect `setHint(shortcutHint(navigator.platform, "K"))` from `lib/platformShortcut.ts`. **Ctrl / ⌘ K** refocuses from anywhere on the page: a `window` keydown effect with the `SeatMap.tsx` predicate (`(meta || ctrl) && !alt && !shift && key === "k"`) → `preventDefault`, `focus()`, `select()`. *Tests:* ct (hint after the effect on a Mac / Windows `navigator.platform`; clear × appears once typed, clears, keeps focus in the field; Ctrl K from a focused row-button refocuses the field); source `reception-source` (`from "@/lib/platformShortcut"`, `role="search"`, `id="reception-main"` on the input); e2e-auth keyboard spec.
3. **List** (PHASE3DS §1.29 — owner ruling, supersedes PHASE2UX §1R.4 item 1 "Avatar 48"; §1R.3; D3-b count copy; D3′). `.sp-recep-list` > `.sp-recep-header` (`span.sp-recep-count[aria-live="polite"]` = **"68 people"** at rest · **"7 matches" / "1 match" / "0 matches"** while typing · **"0 people"** on an empty directory; `span.sp-recep-ext-head` "Ext") + `ul.sp-recep-rows#reception-results[role="listbox"][aria-label="People"]` > `li.sp-recep-row[role="option"][id][aria-selected={locked}][data-highlight?]` `onMouseDown={preventDefault} onClick={lock}` > `span` > `span.sp-recep-name` {name} `<br>` `span.sp-recep-meta` {position · department | "—"} · the seat cell: seat code → `span.sp-recep-seat` (`code-01` text-secondary, **no chip, not a link**); unseated with `floor !== DEFAULT_FLOOR` → `span.cds-tag` {floorTag} (O-10); otherwise an empty `span.sp-recep-seat` (the sheet's `:empty::before` draws "—"); `seatsUnavailable` → empty ("—") on every row · `span.sp-recep-ext` {extension | ""} (empty → "—"). **No avatar anywhere** (`buildInitials` import leaves). **No status mark drawn** (ruling Q-4). Highlighted (the keyboard cursor while typing) = `[data-highlight]` (hover surface + 3px bar); locked (↵) = `aria-selected="true"` (selected surface + bar) — **swapped from the shipped mapping**. `title` on name / meta for the sheet's truncation. Rows 48, header 32, extension column 96 tabular — from the sheet. *Tests:* ct rewrite (`reception-screen`: no `img` / initials node; exactly one `[data-highlight]` while typing and `aria-activedescendant` points at it; `aria-selected="true"` only on the locked row; the Floor tag only where the floor differs; the four count strings incl. "0 matches" and "0 people"); source `reception-source` (`data-highlight`, no `buildInitials`, `role="listbox"` / `role="option"` kept); `touch-target-source` (the Reception comment block: rows and row-buttons are now the sheet's 48 / 40 — no ledger row, no expansion pin).
4. **Readout** (D3-d; D3′; PHASE2UX §1R.4 items 2–7; PHASE3DS §1.29). `section.sp-recep-readout[aria-label="Caller detail"][aria-live="polite"]` (sticky 480, 32 padding, 1px left rule — sheet) > `button.cds-btn.cds-btn--ghost.sp-recep-back` "Back to the list" **first** (shown only under the fold by amendment E; focuses the field and scrolls the list into view) · then either the person block or the waiting copy. Person block: `h2` {name} + `p.sp-recep-role` {position · department} · `div.sp-readout` tile: `span.sp-readout-eyebrow` "Extension" · (`span.sp-readout-numeral` {ext} | `span.sp-readout-none` **"No extension on file"** — never a dash, D3-d) · `span.sp-readout-hint`: while typing `span.sp-kbd`↵ + "to lock"; locked at rest `span.sp-kbd`Esc + "to unlock" · `div.sp-recep-seatline`: `PinIcon` (`mapIcons`) + the D3′ string kept **verbatim** as shipped (`Seat ${seatLabel} · ${floorTag} · ${zone}` / `${floorLabel} — reaches voicemail if away` / "No assigned seat — reaches voicemail if away") — so `reception-source`'s regexes hold unchanged; `seatsUnavailable` → `span.sp-recep-partial` **"Seat unknown right now — the map is still loading."** · `div.sp-recep-fallback` > `h3` "If no answer — same department" + `div.sp-row-buttons` > ≤ 3 `button.cds-btn.cds-btn--ghost` ({name} + `span.sp-row-button-ext` {ext}) `onMouseDown={preventDefault} onClick={lock(colleague)}` (`sameDepartmentFallback` as shipped) · `Link.cds-btn.cds-btn--ghost` **"Show on map"** `href={"/" + withQueryParam("", name)}` (`lib/deepLink.ts`; the D1-d landing) — rendered **only for the locked person**, never for a typing preview. Waiting: `p.sp-recep-waiting` "**Waiting for a call.** Start typing what the caller gives you — a name, department, seat, or extension." **Recent lookups**: `aside.sp-recep-recent[aria-label="Recent lookups"]` **outside** the live region as its own complementary landmark (§1R.7; O-9) > `h3` + `ul` > `li` > `button.cds-btn.cds-btn--ghost` ({name} + `span.sp-recep-ext` {ext}) — up to 4 shown of 5 stored, the locked person excluded, in-memory only (ruled 2026-08-05), hidden while empty. *Tests:* ct (no-extension → the sentence, no "—" in the tile; partial → the seat-unknown line and no voicemail line; Show on map href = `/?q=<name>` and absent while previewing; recents ≤ 4 excl. current, newest first, outside `[aria-live]`; the hint switches ↵ → Esc after a lock; Back to the list focuses the field); source (the three D3′ pins unchanged).
5. **Keyboard** (P3-18; PHASE2UX §1R.7; **ruling Q-1**). On the field: **↓ / ↑** move `highlightIndex` with clamping (never wrap) — results only exist while typing; **↵** while typing with a result → `lock(results[highlight])`; **Esc** — a non-empty query → clear the query (the lock, if any, stays and the readout keeps reading it); an empty query with a lock → unlock (readout → waiting, `?q=` removed). ↵ on an empty query: no-op. **Mousedown never steals focus** — rows, row-buttons, recents and the clear × all `preventDefault`. Ctrl / ⌘ K anywhere (item 2). **Landmarks:** `search` (item 2), `main` (list + readout), `complementary` "Recent lookups" (item 4); the readout is a labelled region inside `main`. **Skip link → the field.** **Tab order:** field → clear × (when shown) → readout actions (Back to the list under the fold · fallback rows · Show on map) → recents. The listbox itself is **not** a tab stop: the combobox / `aria-activedescendant` model reaches it with ↑ ↓ from the field (O-3). *Tests:* ct (clamp; ↵ locks + clears + `aria-selected`; Esc ladder both rungs; ↵ on empty no-op; the four `preventDefault` sites; Ctrl K); e2e-auth `reception-keyboard.spec.ts` (viewer session, 1920: Tab → skip link → the field; type `201` → one match `[data-highlight]`, readout previews "201" with the ↵ hint; ↵ → `aria-selected="true"`, URL `?q=<name>`, hint reads Esc; Esc → unlocked, URL bare; type `zzzz` → "0 matches" + Clear search, readout keeps the last person; click Clear search → field focused, empty; focus a fallback row-button then Ctrl K → the field; mousedown on a row → `document.activeElement` stays the field; `/reception?q=201` lands locked; axe at rest and locked; 1024: one column, "Back to the list" visible and focuses the field).
6. **URL** (D3-c; P2-4's last half). `page.tsx` reads `searchParams.q` (string only) → `initialQuery` prop. Mount effect: results for `initialQuery` — exactly one → `lock()` (writes `?q=<name>`); several → first row highlighted, query kept; zero → the zero state, query kept. `lock()` → `window.history.replaceState(null, "", "/reception" + withQueryParam("", name))`; unlock (Esc rung 2) → `replaceState(null, "", "/reception")`. One writer (`withQueryParam`, `lib/deepLink.ts`); nothing else in the URL. `replaceState`, not `router.replace` (the page is `force-dynamic`; PR 4 §1.37 precedent). *Tests:* ct (the three landing branches; `window.location.search` after lock / unlock; clearing the query alone leaves `?q=`); source (`history.replaceState`, `withQueryParam`, no `useRouter` / `router.push`); e2e-auth (landing + the URL after ↵ / Esc).
7. **States** (P2-5; PHASE2UX §1R.6, §2 rows; D3-e; PHASE3DS §1.29).
   - *First run:* full directory alphabetically; the waiting copy (item 4).
   - *Zero:* in the list body `div.cds-empty` > `h3` "No one matches “xyz”" · `p` "Try a name, department, seat code or extension." · `.cds-empty-actions` > `button.cds-btn.cds-btn--ghost.cds-btn--sm` **Clear search** (`onMouseDown={preventDefault}`); count "0 matches"; the readout keeps the last locked person.
   - *Empty directory:* `div.cds-empty` > `h3` "The directory is empty" · `p` "It fills in when an admin publishes the seat map."; count "0 people"; the search stays; readout waiting.
   - *Loading* (`loading.tsx`): `ReceptionFrame` (header real) > `.sp-recep` > list: a real-looking `.sp-search-lg` (icon + `input.cds-text-input` `readOnly tabIndex={-1}` with the placeholder, inside the `aria-hidden` block) · `.sp-recep-header` (a `.sp-skeleton` line where the count goes · "Ext") · six `.sp-recep-skeleton-row` (three `.sp-skeleton` each) · readout column: one skeleton block; the wrapper `role="status" aria-busy="true"` + sr-only "Loading reception…".
   - *Partial:* `page.tsx` moves to `Promise.allSettled`: employees rejected → `throw` (the boundary); seats rejected → `seatsUnavailable: true`, `buildReceptionDirectory(employees, [])` — every seat cell "—", no Floor tag, the readout's partial seat line, and one `div.cds-notification.cds-notification--warning[role="status"]` above the list: title **"Seat locations didn't load"**, body **"Extensions are up to date. Seat and floor details will show after a reload."** (ruling Q-7).
   - *Error* (`app/(shell)/reception/error.tsx`, "use client"): the admin boundary's mechanics verbatim (`planChunkErrorRecovery` + `sessionStorage` + `window.location.reload()`; heading focus on mount; `onClick={reset}`), on `div.sp-page` > `section.sp-route-card.w-full` `style={{ background: "var(--sp-layer-02)" }}` (the tertiary sits on white — PHASE4BUILD §1.22) > `div.cds-empty` > `h2` (`ErrorGlyph` + **"Reception couldn't load"**, `tabIndex={-1}`) · `p` **"The directory is unchanged — this is a display problem. Try again, or use the seat map's search meanwhile."** · `.cds-empty-actions` > `button.cds-btn.cds-btn--tertiary.cds-btn--md` **Try again** · `Link.cds-btn.cds-btn--ghost.cds-btn--md` `href="/"` **Open the seat map** · `p.sp-digest` "Reference: {digest}" (when present).
   - *Narrow 1024:* sheet **amendment E** (item 10) — one column, readout below the list, not sticky, "Back to the list" shown.
   *Tests:* ct (zero / empty / partial / no-extension / waiting); source `chunk-recovery-boundary-source` (loop gains `../app/(shell)/reception/error.tsx` — both assertions), `pending-state-source` (verify unchanged); rig captures for rest · typing · locked · no-extension · zero · landing · 1280 · 1024 (both themes); partial / empty / loading / error are not drivable on the stack — ct-covered, named as such in the README.
8. **Route cards** (PHASE3DS §2 row "403 card · route error card", §1.29, §5 PR 4/5 landing files; PHASE2UX §1G.5 / §1S.5 "route error — admin voice as shipped"; PHASE4BUILD §1.22 tertiary on white; **ruling Q-2 adds the root boundary and the 404**).
   - `app/(shell)/admin/error.tsx`: `main.flex.min-h-0.flex-1.items-start.justify-center.p-8` > `section.sp-route-card.w-full` (layer-02 inline) > `div.cds-empty` > `h2` (`ErrorGlyph` + "This admin page could not load", `tabIndex={-1}`, focused on mount) · `p` the shipped sentence · actions **Try again** (tertiary, `onClick={reset}`) · **Back to the published map** (ghost `Link` "/") · `p.sp-digest` "Reference: {digest}". `Button` import and `shadow-sp` gone; chunk recovery kept verbatim.
   - `app/(shell)/admin/page.tsx` 403 branch: the PR 4 card verbatim (`.sp-route-card` layer-02 > `.cds-empty` > `h2` "Admin access required" · `p` "…if you need to edit the seat map." · tertiary `Link` "Back to seat map"). The shipped strip with the raster mark and "Megeredchian Law · Seat Planner" goes (O-12); `shadow-sp` gone.
   - `app/error.tsx` (root boundary) and `app/not-found.tsx` (404): the same `.sp-route-card` on layer-02, `ErrorGlyph` on the boundary only, copy as shipped, the boundary keeps its `chunk-recovery` anchors, the 404's action is the shipped one (ghost/tertiary per its verb). Dead `shell-theme` / `sp-zone-*` classes and the `design-system` `Button` import go.
   - `app/global-error.tsx`: the route card in the design system. The file replaces `<html>`, so it imports the stylesheets in `layout.tsx`'s order, the Plex fonts from `app/fonts/plex.ts`, and `THEME_BOOT_SCRIPT`. `<html lang="en" className={fonts}>` > `<body>` (boot script) > `main` (centred, `--sp-background`) > `section.sp-route-card` (layer-02) > `div.cds-empty` > `h2` (`ErrorGlyph` + "The app could not start") · `p` the shipped sentence · **Try again** (tertiary, `reset`) · `p.sp-digest` Reference · the footer line "seats.megeredchianlaw.com · internal use only" (`.sp-digest`). **10 hex leave; the ledger row goes.** The "dependency-free" header comment is amended (O-7). No theme attribute is set → the system state.
   - The management / settings 403 cards: **untouched** (built PR 4).
   - `components/ui/ErrorGlyph.tsx`: the specimen `#i-error20` (circle `currentColor` + cut cross); the cross stroke is `var(--sp-layer-02)` (O-13). Sized and coloured by the sheet's `.sp-route-card .cds-empty h2 svg` rule.
   *Tests:* source `chunk-recovery-boundary-source` (admin + root anchors unchanged), `phase4-token-layer-source` (ledger; the font-bridge pin re-pointed to `app/fonts/plex.ts` — a re-point, not a loosening); e2e-auth `accessibility.spec.ts` (axe on `/admin` as the viewer = the 403 card); rig (the `/admin` 403 both themes; the 404 both themes). `global-error` and the root boundary cannot be driven by any tier — `global-error` verified by a temporary throw in the root layout on the local stack during Task 6, capture kept in `screenshots/pr5/`, the throw reverted before the commit.
9. **`/login` (D4) and `/my-seat` (deviation 12) confirmed unchanged by capture.** No code change on either (ruling Q-3). The runtime audit already captures `/login`; it gains `/my-seat` for the viewer session (1920 both themes). Captured on `main` (v1.75.0) and on the branch with the same rig, same seed, same fonts-ready wait; the PNG pairs are byte-compared in the README. DECISIONS D4 and deviation 12 each gain one dated "confirmed unchanged, PR 5" line.
10. **Sheet amendment E — the Reception narrow fold** (PHASE2UX §1R.6 "Narrow (1024)"; PHASE3DS §2 row 79; the specimen's `#narrow` frame). Appended to block 27, byte-identical in both copies, dated paragraph in PHASE3DS §1.29:
    ```css
    /* Phase 4 PR 5 amendment E (PHASE2UX §1R.6 narrow frame; specimen #narrow): below the asset's 1055
       fold Reception is one column — the readout follows the list, static, ruled above, with the
       "Back to the list" ghost (the explicit back path, D3) shown only there. */
    .sp-recep-back { display: none; }
    @media (max-width: 1055px) {
      .sp-recep { grid-template-columns: minmax(0, 1fr); }
      .sp-recep-readout { position: static; min-height: 0; padding-left: 0; padding-right: 0; box-shadow: inset 0 1px 0 var(--sp-border-subtle-00); }
      .sp-recep-back { display: inline-flex; align-self: flex-start; height: var(--sp-size-md); padding: 0 var(--sp-space-05); }
    }
    ```
    Same fold as amendment C (1055). Zero token change → the contrast line is expected unchanged ("202/202 · no token change").
11. **Carry-ins.**
    - **C-1 `HEX_LEDGER`** → `{ "app/layout.tsx": 1, "components/seat-map/SeatSheet.tsx": 12 }` with both comments re-worded as permanent, reasoned rows (ruling Q-3); `global-error` leaves via item 8.
    - **C-2 `shadow-sp` gone** (PHASE4BUILD §1.5). Four sites: `app/(shell)/admin/error.tsx` → the route card; `app/(shell)/admin/page.tsx` → the 403 route card; `components/seat-map/SeatMapDialogs.tsx` Vacate and Discard draft → the class is deleted, nothing replaces it (layer-01 + 1px border over the overlay, as the asset `.cds-modal` reads). Then `tailwind.config.ts` loses `boxShadow.sp` and its comment; the token test gains "no `shadow-sp` class in `app/**`, `components/**`". Count 40 → 4 → **0**.
    - **C-3 `components/ui/CloseIcon.tsx` retires** (TEST-TRIAGE `close-icon-source` row). The one close glyph is `mapIcons.tsx`'s `CloseIcon` (O-14). Consumers: `EmployeesTable.tsx` toolbar clear ×, `AskPlannerDrawer.tsx` Close Ask Planner, `SeatInspector.tsx` Close inspector + the move-conflict dialog's ×, `SeatMapDialogs.tsx` Delete seat × and Swap × → all import `mapIcons`; the new Reception clear × imports `mapIcons` from the start. Delete `components/ui/CloseIcon.tsx` and `tests/close-icon-source.test.mjs`.
    - **C-4 rigs + specs.** `audit/runtime-audit.mjs`: `PAGE_ROUTES` gains `/reception` (1280 both themes; system state light / dark at 1920 with `null/null`); the viewer pass gains `/my-seat`. `audit/page-states.mjs`: a Reception section under the **viewer** session — rest · typing · locked · recents after two lookups · a no-extension person · zero · `?q=201` landing · 1280 · 1024 with Back to the list; plus the 404; both themes; nothing mutates. `page-frames.spec.ts`: a "Reception frame" block. `header-geometry.spec.ts`: `/reception` on the viewer ladder. `accessibility.spec.ts`: `/reception` at rest and locked (viewer); `/admin` as the viewer. New `reception-keyboard.spec.ts`. Contrast: rerun `generate-pairs.mjs` + `check_contrast.py`, the summary line into PHASE4BUILD §4 — expected "no token change".
12. **Docs + record.** PHASE4BUILD §1: "PR 5 — as planned (v1 + rulings 2026-09-06)" entry pointing at `plans/phase4-pr5-reception.md`, then the O-items as entries (O-8 as a PR 3 obligation found open → PR 5b); §2 ticks P3-4 (Reception half), P3-5 (Reception half — "no mark drawn, §1.29"), P3-18 (wording per Q-1), P2-4 (last half), P2-5; §3 PR 5 row; §4 line; slice log row 5 + new row 5b "not started". PHASE3DS §1.29 amendment E paragraph; §5 item 5 and item 18 corrections; §5 landing note. PHASE2UX §1R.2 amendment line (Q-6); **§1R.4 item 1** amended in place: "~~Avatar 48 ·~~ name `heading-03` 20/28 · … — *Phase 4 PR 5 amendment (2026-09-xx): no avatar, PHASE3DS §1.29 owner ruling*". DECISIONS: D3-e "built PR 5" line; D4 and deviation 12 "confirmed unchanged by capture (PR 5)". TEST-TRIAGE "PR 5 outcomes". `screenshots/pr5/README.md` with provenance (sample data only, viewer + admin seeded accounts, the stack recipe, which states are ct-only).
13. **Version.** On "merge" (owner's word only): squash-merge, annotated tag `v1.76.0`, prune `feat/phase4-reception`, `git pull`, status empty. Slice log row 5 filled.

## Task order (each = its own test cycle + commit on the branch)
0. Branch; save this file as `docs/redesign-v2/phase4/plans/phase4-pr5-reception.md`; commit.
1. Scaffold: PHASE4BUILD §1 stub; `screenshots/pr5/README.md` skeleton; `app/fonts/plex.ts` move + `layout.tsx` import + token-test font-pin re-point; `components/ui/ErrorGlyph.tsx`.
2. Sheet **amendment E** in both copies + PHASE3DS §1.29 paragraph; byte-identical test green.
3. `ReceptionFrame.tsx`; `page.tsx` (`.sp-page` region, `?q=`, `allSettled` partial); `loading.tsx`; `error.tsx`; `chunk-recovery-boundary-source` re-point.
4. `ReceptionScreen.tsx` — search + list + keys + URL (items 2, 3, 5, 6): rewrite `tests/reception-screen.test.mjs` first (failing), then the component; `reception-source` re-point; `touch-target-source` comment.
5. Readout + recents + states (items 4, 7); ct.
6. Route cards (item 8): admin `error.tsx`, `/admin` 403, `global-error.tsx` (+ the temporary-throw capture), `app/error.tsx`, `app/not-found.tsx`.
7. Carry-ins C-2 + C-3: the four `shadow-sp` sites, `tailwind.config.ts`, the seven glyph sites, delete `CloseIcon.tsx` + `close-icon-source`; token test (`HEX_LEDGER` two permanent rows, `shadow-sp` ban).
8. Rigs + specs (C-4): runtime audit, page-states, page-frames, header-geometry, accessibility, `reception-keyboard`.
9. Docs (item 12).
10. Evidence on the Docker stack: `db:start` + `db:seed`, `npm run build && npm run start`, runtime audit (main baseline for `/login` + `/my-seat`, then the branch), page-states, contrast rerun, e2e-auth incl. the new specs; README with provenance.
11. **Stop.** Report the gate output and the preview link. The reviewer's smoke hand-off follows; the PR opens only after the smoke passes.

## Verification (before reporting at Task 11)
```
npm test              # unit + source + db, 0 fail (close-icon-source gone; reception-source re-pointed)
npm run test:ct       # reception-screen rewritten
npm run gate          # lint + typecheck + coverage floors (lib 90/95/80)
npm run build
npm run test:e2e      # viewport-matrix already walks /reception signed-out
npm run test:e2e:auth # Docker stack: publish flow, nav-shell, header-geometry (+/reception), page-frames (+Reception), accessibility (+/reception, +/admin 403), reception-keyboard
node docs/redesign-v2/phase4/audit/runtime-audit.mjs http://localhost:3000 out e2e-admin@example.test <pw>     # + viewer pass for /reception, /my-seat
node docs/redesign-v2/phase4/audit/page-states.mjs http://localhost:3000 out e2e-admin@example.test <pw> e2e-viewer@example.test
node docs/redesign-v2/phase3/contrast/generate-pairs.mjs && python …/check_contrast.py …   # summary line → §4 (expect no token change)
grep -rn "shadow-sp" app components lib tailwind.config.ts   # → nothing
grep -rn "components/ui/CloseIcon" app components tests      # → nothing
```
Brand checklist on the preview: no primary on Reception (none by design); the tertiary Try again on the route cards `#B85C2E` label + outline on white; hover `rgb(143, 69, 33)`; the row bar + focus ring `#B85C2E`; links `#8F4521` / `#E8A07A`; `grep -rn 0f62fe app components lib` → only `carbon-tokens.css`.

## O-items — deviations from the record, and what the code forces
| # | Item | From | Reason / record |
|---|---|---|---|
| O-1 | The list column measures **1008** at 1920, the readout 480 | PHASE2UX §1R.2 "list 1072 · readout 480 · gap 32" | The shipped `.sp-page` is `max-width 1584; padding 0 32` under border-box → a 1520 content box; the specimen renders the same. **Accepted (Q-6)**; PHASE2UX §1R.2 amended |
| O-2 | Esc: a typed query clears first (the lock stays); an empty field unlocks | PHASE3DS §5 item 18 "Esc unlocks then clears" | **Ruled (Q-1)**: PHASE2UX §1R.6's readout-keeps-the-lock principle + the specimen's two hints; item 18 re-worded |
| O-3 | The listbox is not a tab stop | PHASE2UX §1R.7 "Tab → … → list (roving)" | The combobox / `aria-activedescendant` model reaches the rows with ↑ ↓ from the field; a second focus model would be a parallel path |
| O-4 | No status mark on Reception rows; P3-5's Reception half closes as "no mark drawn" | PHASE3DS §5 item 5 lists "Reception rows" as a `SeatMark` consumer | **Ruled (Q-4)**: §1.29, the specimen rows and the wireframe draw the seat code and the Floor tag only |
| O-5 | Copy follows the wireframe / PHASE2UX / DECISIONS where the specimen differs | TEST-TRIAGE "copy follows `reception.html`" | Specimen strings not carried: subtitle, count format, waiting body, zero body, empty-directory pair, error body, digest format |
| O-6 | Partial-state notification copy | PHASE2UX §1R.6 names the notification, not its words | **Ruled (Q-7)**: "Seat locations didn't load" / "Extensions are up to date. Seat and floor details will show after a reload." Warning, `role="status"` |
| O-7 | `global-error.tsx` imports the stylesheets, the font module and the boot string | The file's own "dependency-free" comment | The route card needs the token layer and Plex; CSS and `next/font/local` are build-time; fonts move to `app/fonts/plex.ts` so `layout.tsx` and `global-error` share one declaration; the token test's `variable:` pin re-points |
| O-8 | The map's seven confirm dialogs stay on their Tailwind markup; PR 5 removes only `shadow-sp` and swaps the × glyph | PHASE2UX §3 "Modal (Move / Swap / Delete confirms) → asset `.cds-modal`" (PR 3 landing) — not done in 3b | **Ruled (Q-5)**: a PR 3 obligation found open → **PR 5b** `feat/phase4-map-dialogs` (v1.77.0) before PR 6; slice-log row 5b added |
| O-9 | Recent lookups sit outside the readout's `aria-live` region as their own complementary landmark | Specimen nests recents inside the readout | PHASE2UX §1R.7 names `complementary` "Recent lookups"; announcing the recents on every lock is noise |
| O-10 | The Floor tag renders only when the person's floor differs from `DEFAULT_FLOOR` | §1.29 "Floor 2 tag where the floor differs"; the shipped code tagged any floor | An unseated person on the default floor reads "—", as the specimen's row |
| O-11 | The sticky readout zeroes `--sp-shell-header-h` on itself at `lg` | Sheet `top: calc(--sp-shell-header-h + 16)` assumes a scrolling document | In the shell the pane scrolls at `lg`; PR 4 §1.37 did the same for the tab strip — no sheet change |
| O-12 | The `/admin` 403 card drops its raster-mark strip | Shipped card (pre-Phase 4) | The route card is the asset empty state; the shell header already carries the product name; `megeredchian-mark.png` keeps its `/login` consumer (D4) |
| O-13 | `ErrorGlyph`'s cut cross is `--sp-layer-02` | Specimen `#i-error20` strokes `--sp-layer-01` | The route card is layer-02 by the §1.22 tertiary rule |
| O-14 | `mapIcons.tsx` is the shared glyph module | `components/ui/CloseIcon.tsx` was the "one close glyph" | PR 4 already imports `mapIcons` cross-area; a rename to `components/ui/icons.tsx` is a PR 6 candidate |
| O-15 | `Promise.allSettled` on the Reception page; employees rejected still throws | Shipped `Promise.all` | The partial state (P2-5) needs the seats query to fail alone |
| O-16 | `SeatSheet.tsx` keeps its 12 hex as a permanent ledger row | PHASE4BUILD §1.19 O5 "until PR 5" | **Ruled (Q-3)**: `/my-seat` has no Phase 2/3 design; a re-skin would be a Phase 4 design decision; deviation 12 |
| O-17 | `app/error.tsx` and `app/not-found.tsx` join the route-card pass | Brief named the admin boundary and `global-error` | **Ruled (Q-2)**: same component, no design decision, "route surfaces" per the slice log |

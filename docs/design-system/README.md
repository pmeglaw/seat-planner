# docs/design-system/ — superseded (Phase 4 close-out, v2.0.0, 2026-09-08)

Phase 0 history: the audits (`AUDIT.md`, `AUDIT-2.md`), the first plan (`PLAN.md`), the palette and PASS1 token
assessments, and the read-path assessment with its two captures. The redesign that replaced them lives in
`docs/redesign-v2/` — PHASE1IA → PHASE2UX → PHASE3DS (the CSS deliverable) → phase4/PHASE4BUILD (the code), with
`DECISIONS.md` as the ruling record — and none of the documents here is an input to it (the Phase 4 hand-off listed
`AUDIT*.md`, `PLAN.md` and the `docs/redesign` branch's `shell-reference.html` as off-limits). Kept, not deleted:
`READ-PATH-ASSESSMENT.md` is still cited by `CLAUDE.md`'s hardware-target line (dual 27" FHD; the 24–25 Aug rulings
were laptop-premised — its addendum).

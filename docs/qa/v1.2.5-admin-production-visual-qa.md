# v1.2.5 Admin Production Visual QA

## Date/Time

- QA completed: 2026-05-22 12:01:17 -07:00
- Handoff regenerated: 2026-05-22 11:40:46 -07:00

## Git Branch And Commit

- Branch: `v1.2.5-admin-production-visual-qa`
- Commit: `fc90a7144e205925008980570dc6fb24fe72b688`

## Summary

Performed a production-focused visual QA and data-integrity review using `tools/seat-planner-improvement-loop/output/codex_handoff.md` from the v1.2.4 handoff harness. No production runtime bug was found. The pass is documentation-only: no app runtime files, auth behavior, route behavior, publish workflow, production dependencies, or Supabase schema files were changed.

Final recommendation: ready to merge.

## What Was Verified

- Authenticated production `/` loads the published viewer map.
- Authenticated production `/admin` loads the admin draft map.
- Authenticated production `/admin/management` loads management tabs and publish history.
- Unauthenticated local and production route checks redirect to login with the expected `next` values.
- Viewer route copy remains `Select a seat to view assignment details.`
- Admin route copy remains `Select a seat to assign or update employee details.`
- Viewer route does not expose admin controls.
- Admin assigned-seat and open-seat inspector actions render as expected.
- Publish controls remain behind admin advanced tools and include safe publish copy.
- Draft/published data separation is preserved in runtime code and read-only aggregate live data checks.
- Removed legacy messaging tables are not referenced by runtime code.
- Mobile viewer/admin map views remain usable with intended map-level horizontal scrolling.

## Browser Routes Checked

Local unauthenticated checks against `http://127.0.0.1:3000`:

- `/` redirected to `/login?next=/`.
- `/admin` redirected to `/login?next=/admin`.
- `/admin/management` redirected to `/login?next=/admin/management`.

Production unauthenticated checks before admin sign-in:

- `/` redirected to `/login?next=/`.
- `/admin` redirected to `/login?next=/admin`.
- `/admin/management` redirected to `/login?next=/admin/management`.

Production authenticated checks:

- `/` rendered `Published seat map` with 61 markers and loaded the floor plan image.
- `/admin` rendered `Draft seat map` with 61 markers and loaded the floor plan image.
- `/admin/management` rendered management UI with Employees, Departments, Zones, and Publish History tabs.

## Authenticated Admin Observations

- `/admin` rendered `Admin draft view`, `Draft seat map`, `Management`, and `Advanced`.
- The floor plan image loaded successfully.
- Seat count was plausible and matched live aggregate data: 61 draft seats.
- An assigned seat opened the inspector and showed `Update Assignment` and `Vacate Seat`.
- An open seat opened the inspector and showed `Assign Seat` without `Update Assignment` or `Vacate Seat`.
- Assigned-user search was verified with keyboard entry: a known assigned user query returned a visible result, no no-match state, and the matching marker was not dimmed.
- A Vacate confirmation was triggered during QA and cancelled/recovered with Escape/reload; live aggregate counts after recovery still showed 8 draft assigned seats and 8 published assigned seats.
- Advanced tools showed draft-map tools, publish controls, and protected custom-seat deletion copy.

## Viewer Observations

- Authenticated `/` rendered `Published viewer view` and `Published seat map`.
- Viewer copy was present: `Select a seat to view assignment details.`
- Admin copy was absent from the viewer route.
- Refined control scan found no forbidden viewer controls: `Assign Seat`, `Update Assignment`, `Vacate Seat`, `Publish Draft Map`, `Advanced`, `Management`, `Add Custom Seat`, or `Move Selected Seat`.
- Selecting a viewer marker opened a read-only seat assignment panel with no admin form actions.

## Management Page Observations

- `/admin/management` rendered without runtime error text.
- Employees, Departments, Zones, and Publish History tabs were present.
- Employees view showed the admin management shell, summary metrics, employee search, and editor area.
- Departments tab opened successfully.
- Zones tab opened successfully.
- Publish History tab opened successfully and showed recent publish history content, including latest/previous publish states.
- No obvious empty-state or layout failure was observed.

## Data Integrity Observations

Runtime code review:

- `app/page.tsx` queries `seats` with `layer = "published"` for the viewer map.
- `app/admin/page.tsx` queries `seats` with `layer = "draft"` for the admin map.
- `app/admin/management/page.tsx` queries `seats` with `layer = "draft"` for management.
- `app/actions.ts` gates mutations through `requireAdmin()` and updates draft seats with `layer = "draft"` filters.
- Runtime search found no references to removed legacy messaging objects in `app`, `components`, `lib`, `middleware.ts`, or `tests`.

Read-only live aggregate check:

- Draft seats: 61.
- Published seats: 61.
- Draft assigned seats: 8.
- Published assigned seats: 8.
- Active employees: 8.
- Active departments: 8.
- Active zones: 7.
- Publish events: 2.
- Legacy messaging tables present: none.
- Publish functions present: `publish_seat_map`; stale `publish_draft_seats` absent.

## Publish Workflow Observations

- Publish UI remains inside admin-only Advanced tools.
- Advanced drawer copy states publishing copies the current draft map to the viewer-facing map after a confirmation summary.
- `SeatMap.tsx` publish confirmation copy includes viewer-facing impact, undo/redo clearing, seat totals, and Cancel guidance.
- No publish action was confirmed during QA.
- Live `publish_events` count remained 2 after QA.

## Assignment/Vacate Workflow Observations

- Assigned inspector showed `Update Assignment` and `Vacate Seat`.
- Open-seat inspector showed `Assign Seat`.
- Vacate was not confirmed. After cancelling/recovering from the native confirmation, aggregate assignment counts remained unchanged.
- No destructive save was performed.

## Mobile Smoke Test Observations

Viewport: 390 x 844.

- Viewer `/` rendered `Published seat map`, viewer copy, 61 markers, and an intended map-level horizontal scroller.
- Admin `/admin` rendered `Draft seat map`, admin copy, 61 markers, and an intended map-level horizontal scroller.
- Document-level overflow was not observed outside intended map scrolling; measured document/body overflow was negative due to scrollbar sizing.
- Map scroller delta was 565px on both viewer and admin mobile checks, matching the intended horizontally scrollable map behavior.

## Issues Found

No production runtime issues found.

Notes:

- Browser automation could not type through the normal fill/type helper because the in-app virtual clipboard was unavailable. Search QA was completed with keypress entry instead.
- Native confirmation dialogs can temporarily block automation. The Vacate confirmation was cancelled/recovered, and read-only aggregate checks confirmed no data changed.

## Code Changes Made

Documentation only:

- Added `docs/qa/v1.2.5-admin-production-visual-qa.md`.

No production runtime files changed.

## Validation Commands And Results

- `npm run qa:handoff` - passed; regenerated `tools/seat-planner-improvement-loop/output/codex_handoff.md`.
- `npm test` - passed; 27 tests passed.
- `npm run typecheck` - passed.
- `npm run lint` - passed.
- `npm run build` - passed; Next.js production build completed successfully.

## Required Change Flags

- Files changed: `docs/qa/v1.2.5-admin-production-visual-qa.md`.
- Production runtime files changed: no.
- Supabase schema changed: no.
- Viewer/admin behavior changed: no.
- Auth behavior changed: no.
- Publish workflow changed: no.
- New production dependencies added: no.
- OpenAI API usage added: no.
- Runtime AI behavior added: no.

## Final Recommendation

Ready to merge.
